let animales = ["Perro", "Gato", "Elefante", "Calamar", "Lobo", "Cocodrilo", "Tigre"];

for (let i = 0; i < animales.length; i++) 
    {
      if (animales[i].length > 5) 
      {
        console.log(animales[i]);
      }
    }